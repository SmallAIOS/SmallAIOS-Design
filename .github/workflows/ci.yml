name: SmallAIOS CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: '0 6 * * 1'  # Weekly Monday 06:00 UTC (Miri)
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"
  BUILD_STD: "-Z build-std=core,compiler_builtins,alloc -Z build-std-features=compiler-builtins-mem"

jobs:
  check-format:
    name: Format Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: Clippy Lint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: clippy, rust-src
      # Crate/feature list is single-sourced in ci/test-matrix.toml so lint
      # coverage tracks test coverage (spec: ci-test-matrix).
      - run: cargo clippy $(python3 scripts/test_matrix.py --emit clippy-args) -- -D warnings

  build-x86_64:
    name: Build x86-64 Kernel
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: x86_64-unknown-none
      - run: cargo build --target x86_64-unknown-none -p smallaios-arch-x86_64 $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings -C link-arg=-Tarch/x86_64/linker.ld -C code-model=kernel"

  build-aarch64:
    name: Build AArch64 Kernel
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: aarch64-unknown-none
      - run: cargo build --target aarch64-unknown-none -p smallaios-arch-aarch64 $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings -C link-arg=-Tarch/aarch64/linker.ld"

  build-jetson:
    name: Build Jetson Nano (Tegra X1)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: aarch64-unknown-none
      - run: >-
          cargo build --target aarch64-unknown-none
          -p smallaios-arch-aarch64 --no-default-features --features tegra-x1
          $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings -C link-arg=-Tarch/aarch64/linker-tegra.ld"

  build-riscv64:
    name: Build RISC-V Kernel
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: riscv64gc-unknown-none-elf
      - run: cargo build --target riscv64gc-unknown-none-elf -p smallaios-arch-riscv64 $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings -C link-arg=-Tarch/riscv64/linker.ld"

  # The crate/feature matrix is single-sourced in ci/test-matrix.toml
  # (spec: ci-test-matrix). This job verifies every workspace member is
  # classified there and emits the group matrix for test-matrix-groups.
  test-matrix-setup:
    name: Test Matrix Verify
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.emit.outputs.matrix }}
    steps:
      - uses: actions/checkout@v7
      - name: Verify workspace coverage
        run: python3 scripts/test_matrix.py --verify
      - id: emit
        # `default` runs as the long-standing required "Unit Tests" check
        # below; the matrix job carries the remaining groups.
        run: echo "matrix=$(python3 scripts/test_matrix.py --emit gha --except default)" >> "$GITHUB_OUTPUT"

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - run: python3 scripts/test_matrix.py --run default

  test-matrix-groups:
    name: Unit Tests (${{ matrix.group }})
    needs: test-matrix-setup
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.test-matrix-setup.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - run: python3 scripts/test_matrix.py --run "${{ matrix.group }}"

  cuda-check:
    name: CUDA Feature Check (cargo check --features cuda)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # Cheap gate that catches Send/Sync regressions in the GPU code path.
    # `cargo check` does not link, so we don't need libcudart on the
    # runner — only the Rust toolchain. This unblocks GPU-feature
    # development without requiring a GPU CI host.
    #
    # We do NOT use `--workspace` here because `arch/aarch64` and
    # `arch/riscv64` carry unguarded `core::arch::asm!` blocks that
    # only assemble for their own target_arch. The `Unit Tests` job
    # has the same constraint and uses the same curated-list pattern.
    # The list below covers every workspace crate that touches the
    # `cuda` / `nvidia_gpu` features either directly or via a
    # dependency edge — adding a new crate that imports `cuda::*`
    # means adding it here too.
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: cargo check --features cuda,nvidia_gpu
        run: |
          cargo check \
            -p smallaios-onnx-rt \
            -p smallaios-container \
            -p smallaios-arch-nvidia \
            -p smallaios-compute \
            --features cuda,nvidia_gpu

  jetson-image-build:
    name: Jetson Image Build (advisory, QEMU)
    runs-on: ubuntu-latest
    # Advisory until a self-hosted Jetson runner is available — there's no way
    # to run a real Tegra runtime test on GitHub-hosted runners. This job
    # at least catches Dockerfile.jetson syntactic / dependency rot under
    # QEMU emulation.
    continue-on-error: true
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v4
      - name: Build Dockerfile.jetson under QEMU (linux/arm64)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.jetson
          platforms: linux/arm64
          push: false
          load: false
          cache-from: type=gha,scope=jetson
          cache-to: type=gha,mode=max,scope=jetson

  test-formal-gate:
    name: Unit Tests (formal-gate)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Test with formal-gate enabled
        run: >-
          cargo test
          -p smallaios-security --features formal-gate
          -p smallaios-ipc --features formal-gate
          -p smallaios-onnx-rt --features formal-gate
          -p smallaios-container --features formal-gate
      - name: Test without formal-gate (default)
        run: >-
          cargo test
          -p smallaios-security
          -p smallaios-ipc
          -p smallaios-onnx-rt
          -p smallaios-container

  test-verified-boot:
    name: Unit Tests (verified-boot)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Test with verified-boot enabled
        run: >-
          cargo test
          -p smallaios-kernel --features verified-boot,no-global-alloc
          -p smallaios-security --features verified-boot
          -p smallaios-onnx-rt --features verified-boot
      - name: Clippy with verified-boot enabled
        run: >-
          cargo clippy
          -p smallaios-kernel --features verified-boot,no-global-alloc
          -p smallaios-security --features verified-boot
          -p smallaios-onnx-rt --features verified-boot
          -- -D warnings

  riscv-qemu-smoke:
    name: RISC-V QEMU Smoke Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: build-riscv64
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: riscv64gc-unknown-none-elf
      - name: Install QEMU
        run: sudo apt-get update && sudo apt-get install -y qemu-system-misc
      - name: Build RISC-V kernel
        run: cargo build --release --target riscv64gc-unknown-none-elf -p smallaios-arch-riscv64 $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings -C link-arg=-Tarch/riscv64/linker.ld"
      - name: Boot in QEMU (timeout 30s)
        run: |
          timeout 30 qemu-system-riscv64 \
            -machine virt -cpu rv64 -m 512M -nographic \
            -bios default \
            -kernel target/riscv64gc-unknown-none-elf/release/smallaios-riscv64 \
            -serial stdio 2>&1 | tee boot.log || true
          echo "=== Boot log ==="
          cat boot.log
          echo "=== RISC-V QEMU smoke test completed ==="

  aarch64-qemu-smoke:
    name: AArch64 QEMU Smoke Test (Jetson KVM path)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    # No `needs:` — this job rebuilds the AArch64 kernel from source so it
    # exercises the same RUSTFLAGS / target / linker-script path the Jetson
    # quickstart documents. Running independently lets it parallel with
    # `build-aarch64` rather than serialize behind it.
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: aarch64-unknown-none
      - name: Install QEMU
        run: sudo apt-get update && sudo apt-get install -y qemu-system-arm
      - name: Build AArch64 kernel (release)
        run: cargo build --release --target aarch64-unknown-none -p smallaios-arch-aarch64 $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings -C link-arg=-Tarch/aarch64/linker.ld"
      - name: Boot in QEMU virt with GICv3 (TCG, timeout 30s)
        run: |
          # No KVM on GitHub runners — use TCG. Same machine + GIC version as
          # the Jetson recipe path so the kernel exercises the same init shape.
          # Asserts the boot banner that kernel_main() prints unconditionally.
          timeout 30 qemu-system-aarch64 \
            -M virt,gic-version=3 -cpu cortex-a72 -m 1G -nographic \
            -kernel target/aarch64-unknown-none/release/smallaios-aarch64 \
            -serial mon:stdio 2>&1 | tee boot.log || true
          echo "=== Boot log ==="
          cat boot.log
          echo "=== Asserting Phase 1 boot banner ==="
          grep -q "SmallAIOS" boot.log || { echo "FAIL: 'SmallAIOS' banner missing"; exit 1; }
          grep -q "BSS cleared" boot.log || { echo "FAIL: '[boot] BSS cleared' marker missing — kernel did not reach BSS-clear stage"; exit 1; }
          echo "PASS: AArch64 kernel boots under QEMU virt + GICv3 (Phase 1 path validated under TCG)"

  # ──────────────────────────────────────────────────────────────────────
  # Jetson Orin UEFI build — advisory.
  #
  # Builds the `aarch64-unknown-uefi` PE32+ artifact (`smallaios-uefi.efi`)
  # that boots on real Jetson Orin hardware via the USB stick path
  # documented in `docs/jetson-orin-uefi-boot.md`. Catches PE/COFF + UEFI
  # entry regressions even without a self-hosted Jetson runner.
  #
  # `continue-on-error: true` keeps this advisory while Phase 2 is still
  # in interim mode (`efi_main` skips ExitBootServices; the kernel-side
  # post-EBS UART driver is a follow-up sub-PR). Once that lands and the
  # whole boot path is "real," promote this to a gate by removing
  # `continue-on-error` and adding the job to `change-gates.needs`.
  #
  # Notes:
  #   - The original task wording (#2.21) referenced `-p smallaios-kernel`,
  #     but the bin target lives in `arch/aarch64/Cargo.toml` (`-p
  #     smallaios-arch-aarch64 --bin smallaios-uefi`). Same correction
  #     that landed in sub-PR 2c's `build-kernel-uefi` Justfile recipe.
  #   - OVMF QEMU smoke (task 2.22) is implemented as the sibling
  #     `jetson-orin-ovmf-smoke` job below: it boots this same .efi under
  #     emulated UEFI and asserts the `efi_main` banner reaches the
  #     serial console — catching PE/COFF + UEFI-entry regressions a
  #     compile-only check cannot. The OVMF source was resolved to the
  #     `qemu-efi-aarch64` Ubuntu package (AAVMF); see that job's header
  #     for the firmware/CPU/image rationale.
  build-jetson-orin-uefi:
    name: Build Jetson Orin UEFI (advisory)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: aarch64-unknown-uefi
      - name: Build smallaios-uefi.efi
        run: >-
          cargo build --release --target aarch64-unknown-uefi
          -p smallaios-arch-aarch64 --bin smallaios-uefi
          --no-default-features --features tegra234
          $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings"
      - name: Verify PE32+ EFI artifact
        run: |
          set -euo pipefail
          EFI=target/aarch64-unknown-uefi/release/smallaios-uefi.efi
          ls -la "$EFI"
          file "$EFI"
          # Sanity check: file(1) on Linux reports the PE32+/EFI marker.
          file "$EFI" | grep -qE "PE32\+ executable.*EFI" || {
            echo "FAIL: $EFI is not a recognized PE32+ EFI binary" >&2
            exit 1
          }
          echo "PASS: smallaios-uefi.efi built and recognized as PE32+ EFI (Aarch64)"

  # ──────────────────────────────────────────────────────────────────────
  # Jetson Orin OVMF QEMU smoke — advisory (task 2.22).
  #
  # Boots the `smallaios-uefi.efi` PE32+ artifact under emulated UEFI on
  # `qemu-system-aarch64 -M virt` and asserts the early `efi_main` banner
  # reaches the serial console. Catches PE/COFF layout + UEFI entry-point
  # regressions in CI without a self-hosted Jetson runner — the on-J4012
  # boot is task 2.16 (a later on-hardware sub-PR).
  #
  # Why this is meaningful even though the kernel is interim-mode:
  # `efi_main` prints "Hello, world from SmallAIOS on Tegra234" via UEFI
  # `con_out` *before* DTB lookup / GetMemoryMap / ExitBootServices and
  # before any Tegra234 MMIO. OVMF routes con_out to the serial line, so
  # the assertion exercises firmware → PE loader → our `efi_main`
  # independent of the firewalled-MMIO post-EBS quirks that gate
  # real-hardware output (tasks 2.10/2.11).
  #
  # Design decisions resolved for this sub-PR (the task left these open):
  #   - OVMF source: the `qemu-efi-aarch64` Ubuntu package (AAVMF) —
  #     pinned by the runner image, no download or from-source build.
  #     Booted as the canonical CODE(ro)+VARS(rw) pflash pair.
  #   - Disk: the same FAT32 superfloppy the real USB stick uses, built
  #     inline with mtools (mirrors the `build-jetson-usb-image` Justfile
  #     recipe; CI never invokes `just`, so the steps are inlined).
  #     Highest fidelity — identical artifact + layout to hardware.
  #   - CPU: `cortex-a76`, not the Orin's `cortex-a78`. A banner smoke is
  #     silicon-agnostic and a76 is the newest core reliably supported
  #     across the QEMU TCG versions on `ubuntu-latest`.
  #
  # `continue-on-error: true` keeps this advisory while Phase 2 is in
  # interim mode. Promote to a gate (remove continue-on-error + add to
  # `change-gates.needs`) once the post-EBS UART lands and the whole
  # boot path is "real" — same promotion seam as build-jetson-orin-uefi.
  jetson-orin-ovmf-smoke:
    name: Jetson Orin OVMF Smoke (advisory)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: aarch64-unknown-uefi
      - name: Install QEMU + AArch64 UEFI firmware + mtools
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-arm qemu-efi-aarch64 mtools
      - name: Build smallaios-uefi.efi
        run: >-
          cargo build --release --target aarch64-unknown-uefi
          -p smallaios-arch-aarch64 --bin smallaios-uefi
          --no-default-features --features tegra234
          $BUILD_STD
        env:
          RUSTFLAGS: "-D warnings"
      - name: Build bootable FAT32 image (mtools — matches the USB-stick path)
        run: |
          set -euo pipefail
          EFI=target/aarch64-unknown-uefi/release/smallaios-uefi.efi
          file "$EFI" | grep -qE "PE32\+ executable.*EFI" || {
            echo "FAIL: $EFI is not a recognized PE32+ EFI binary" >&2
            exit 1
          }
          mkdir -p build
          IMG=build/smallaios-jetson-usb.img
          rm -f "$IMG"
          # 64 MB is ample for a sub-MB .efi and boots faster than the
          # 256 MB default the Justfile recipe uses for real USB sticks.
          dd if=/dev/zero of="$IMG" bs=1M count=64 status=none
          mformat -F -v SMALLAIOS -i "$IMG" ::
          mmd  -i "$IMG" ::/EFI
          mmd  -i "$IMG" ::/EFI/BOOT
          mcopy -i "$IMG" "$EFI" ::/EFI/BOOT/BOOTAA64.EFI
          mdir -i "$IMG" ::/EFI/BOOT
      - name: Boot under OVMF on QEMU virt (TCG, timeout 120s)
        run: |
          set -uo pipefail
          # Canonical AArch64 UEFI pflash pair: CODE read-only, a
          # writable copy of VARS (fresh NVRAM → edk2 default boot
          # enumerates FAT volumes and runs \EFI\BOOT\BOOTAA64.EFI).
          cp /usr/share/AAVMF/AAVMF_VARS.fd vars.fd
          timeout 120 qemu-system-aarch64 \
            -M virt -cpu cortex-a76 -m 1G -nographic \
            -drive if=pflash,format=raw,readonly=on,file=/usr/share/AAVMF/AAVMF_CODE.fd \
            -drive if=pflash,format=raw,file=vars.fd \
            -drive if=none,format=raw,file=build/smallaios-jetson-usb.img,id=hd0 \
            -device virtio-blk-pci,drive=hd0 \
            -serial mon:stdio -no-reboot 2>&1 | tee boot.log || true
          echo "=== Boot log ==="
          cat boot.log
          echo "=== Asserting efi_main UEFI banner ==="
          grep -q "Hello, world from SmallAIOS on Tegra234" boot.log || {
            echo "FAIL: efi_main banner missing — PE/COFF load or UEFI entry regressed under emulated UEFI"
            exit 1
          }
          echo "PASS: smallaios-uefi.efi boots under emulated UEFI; efi_main banner observed"

  image-size:
    name: Image Size Check (<15 MB)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: [build-x86_64, build-aarch64, build-riscv64, build-jetson]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
          targets: x86_64-unknown-none, aarch64-unknown-none, riscv64gc-unknown-none-elf
      - name: Build all release kernels
        run: |
          RUSTFLAGS="-D warnings -C link-arg=-Tarch/x86_64/linker.ld -C code-model=kernel" \
            cargo build --release --target x86_64-unknown-none -p smallaios-arch-x86_64 $BUILD_STD
          RUSTFLAGS="-D warnings -C link-arg=-Tarch/aarch64/linker.ld" \
            cargo build --release --target aarch64-unknown-none -p smallaios-arch-aarch64 $BUILD_STD
          RUSTFLAGS="-D warnings -C link-arg=-Tarch/riscv64/linker.ld" \
            cargo build --release --target riscv64gc-unknown-none-elf -p smallaios-arch-riscv64 $BUILD_STD
          RUSTFLAGS="-D warnings -C link-arg=-Tarch/aarch64/linker-tegra.ld" \
            cargo build --release --target aarch64-unknown-none \
            -p smallaios-arch-aarch64 --no-default-features --features tegra-x1 $BUILD_STD
      - name: Check binary sizes
        run: |
          MAX=$((15 * 1024 * 1024))
          fail=0
          for bin in \
            target/x86_64-unknown-none/release/smallaios-x86_64 \
            target/aarch64-unknown-none/release/smallaios-aarch64 \
            target/riscv64gc-unknown-none-elf/release/smallaios-riscv64; do
            if [ -f "$bin" ]; then
              size=$(stat --format=%s "$bin")
              kb=$((size / 1024))
              name=$(basename "$bin")
              echo "$name: ${kb} KB ($(( kb / 1024 )) MB)"
              if [ "$size" -gt "$MAX" ]; then
                echo "FAIL: $name exceeds 15 MB limit"
                fail=1
              fi
            fi
          done
          if [ "$fail" -eq 1 ]; then exit 1; fi
          echo "All binaries within 15 MB limit."

  tla-verify:
    name: TLA+ Formal Verification
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: 17
      - name: Download TLA+ tools
        run: |
          mkdir -p /tmp/tla
          curl -sSL -o /tmp/tla/tla2tools.jar \
            https://github.com/tlaplus/tlaplus/releases/download/v1.8.0/tla2tools.jar
      - name: Run TLC on all protocol models
        run: |
          models=(
            CanArbitration
            Arinc429Scheduler
            AfdxVirtualLink
            Mil1553Protocol
            SpaceWireLink
            DdsReliableDelivery
            DdsDiscovery
            QuicFlowControl
            QuicMigration
            BuddyAllocator
            Scheduler
            I2CArbitration
            SPIProtocol
            GPIOInterrupt
            UARTFlowControl
            CSIFrameBuffer
            I2SRingBuffer
            SecurityGate
            PolicyUpdate
            USBEnumeration
            XhciTransferRing
            IQRingBuffer
          )
          failed=()
          timed_out=()
          for model in "${models[@]}"; do
            echo "=== Verifying $model ==="
            set +e
            timeout 300 java -Xmx4g -cp /tmp/tla/tla2tools.jar tlc2.TLC \
              formal/tla/${model}.tla \
              -config formal/tla/${model}.cfg \
              -workers auto \
              -deadlock
            rc=$?
            set -e
            if [ $rc -eq 124 ]; then
              echo "WARNING: $model timed out (5 min limit) — state space too large for CI"
              timed_out+=("$model")
            elif [ $rc -ne 0 ]; then
              echo "FAIL: $model verification failed (exit code $rc)"
              failed+=("$model")
            else
              echo "OK: $model verified"
            fi
          done
          echo ""
          echo "=== TLA+ Summary ==="
          echo "Timed out (${#timed_out[@]}): ${timed_out[*]:-none}"
          echo "Failed (${#failed[@]}): ${failed[*]:-none}"
          if [ ${#failed[@]} -gt 0 ]; then
            echo "FAIL: ${#failed[@]} model(s) had verification errors"
            exit 1
          fi
          echo "All TLA+ models verified or bounded by timeout."

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: test
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src, llvm-tools
      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov
      - name: Generate per-crate coverage reports
        run: |
          CRATES=(kernel security onnx-rt ipc net posix container bus peripheral usb sdr auth mgmt fs)
          EXTRA_FLAGS=""
          for crate in "${CRATES[@]}"; do
            pkg="smallaios-${crate}"
            outfile="lcov-${crate}.info"
            flags=""
            if [ "$crate" = "peripheral" ]; then
              flags="--features smallaios-peripheral/full-peripheral"
            fi
            if [ "$crate" = "kernel" ]; then
              # Integration tests under kernel (e.g. model_syscall_conformance)
              # need test-mocks for mock providers and no-global-alloc so the
              # host allocator is used instead of the static KernelAllocator.
              flags="--features smallaios-kernel/test-mocks,smallaios-kernel/no-global-alloc"
            fi
            cargo llvm-cov --lcov --output-path "$outfile" -p "$pkg" $flags
          done
      - name: Upload per-crate coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-kernel.info
          flags: kernel
          fail_ci_if_error: false
      - name: Upload security coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-security.info
          flags: security
          fail_ci_if_error: false
      - name: Upload onnx-rt coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-onnx-rt.info
          flags: onnx-rt
          fail_ci_if_error: false
      - name: Upload net coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-net.info
          flags: net
          fail_ci_if_error: false
      - name: Upload ipc coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-ipc.info
          flags: ipc
          fail_ci_if_error: false
      - name: Upload container coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-container.info
          flags: container
          fail_ci_if_error: false
      - name: Upload bus coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-bus.info
          flags: bus
          fail_ci_if_error: false
      - name: Upload peripheral coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-peripheral.info
          flags: peripheral
          fail_ci_if_error: false
      - name: Upload usb coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-usb.info
          flags: usb
          fail_ci_if_error: false
      - name: Upload sdr coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-sdr.info
          flags: sdr
          fail_ci_if_error: false
      - name: Upload posix coverage
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-posix.info
          flags: posix
          fail_ci_if_error: false
      - name: Merge all coverage for SonarCloud
        run: cat lcov-*.info > lcov.info
      - name: Convert LCOV to SonarCloud generic format
        run: python3 scripts/lcov-to-sonar.py lcov.info > sonar-coverage.xml
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-sonar
          path: sonar-coverage.xml
          retention-days: 1

  sonarcloud:
    name: SonarCloud Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    needs: coverage
    if: github.event_name == 'pull_request' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Download coverage artifact
        uses: actions/download-artifact@v8
        with:
          name: coverage-sonar
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  docker-build-local:
    name: Docker Build (Local)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Build Docker image (CPU-only)
        run: docker build -t smallaios:ci-test .
      - name: Check image size
        run: |
          size=$(docker image inspect smallaios:ci-test --format='{{.Size}}')
          max=$((15 * 1024 * 1024))
          mb=$(( size / 1024 / 1024 ))
          echo "Docker image size: ${mb} MB (${size} bytes)"
          if [ "$size" -gt "$max" ]; then
            echo "FAIL: Docker image exceeds 15 MB"
            exit 1
          fi
          echo "PASS: Docker image within 15 MB limit"

  docker-build-arm64:
    name: Docker Build (ARM64)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Set up QEMU for ARM64 emulation
        uses: docker/setup-qemu-action@v4
        with:
          platforms: arm64
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build ARM64 container image
        run: docker buildx build --platform linux/arm64 -t smallaios:arm64-ci --load .
      - name: Verify image architecture
        run: |
          arch=$(docker image inspect smallaios:arm64-ci --format='{{.Architecture}}')
          echo "Image architecture: ${arch}"
          if [ "$arch" != "arm64" ]; then
            echo "FAIL: expected arm64, got ${arch}"
            exit 1
          fi
          echo "PASS: ARM64 image built successfully"
      - name: Check image size
        run: |
          size=$(docker image inspect smallaios:arm64-ci --format='{{.Size}}')
          max=$((15 * 1024 * 1024))
          mb=$(( size / 1024 / 1024 ))
          echo "Docker image size: ${mb} MB (${size} bytes)"
          if [ "$size" -gt "$max" ]; then
            echo "FAIL: Docker image exceeds 15 MB"
            exit 1
          fi
          echo "PASS: ARM64 Docker image within 15 MB limit"
      - name: Smoke test ARM64 container (health check)
        run: |
          docker run --rm -d --name arm64-smoke -p 8080:8080 smallaios:arm64-ci
          sleep 3
          response=$(curl -sf http://localhost:8080/healthz || echo "FAIL")
          docker stop arm64-smoke || true
          echo "Health response: ${response}"
          if echo "$response" | grep -q "healthy"; then
            echo "PASS: ARM64 container health check passed"
          else
            echo "FAIL: ARM64 container health check failed"
            exit 1
          fi

  docker-build-gpu:
    name: Docker Build (GPU)
    runs-on: ubuntu-latest
    # GPU Docker build requires NVIDIA CUDA devel base image (~5 GB pull).
    # Only runs on manual dispatch or scheduled — too slow for every PR.
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'schedule'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build GPU Dockerfile (full build)
        run: docker build -f Dockerfile.cuda -t smallaios:gpu-ci .
      - name: Verify GPU image built
        run: |
          arch=$(docker image inspect smallaios:gpu-ci --format='{{.Architecture}}')
          echo "GPU image architecture: ${arch}"
          echo "PASS: GPU Dockerfile builds successfully"

  semver-check:
    name: Semver PR Title Check
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v7
      - name: Validate PR title follows conventional commits
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          bump=$(./scripts/check-pr-semver.sh "$PR_TITLE")
          echo "PR title: $PR_TITLE"
          echo "Semver bump: $bump"
          echo "bump=$bump" >> "$GITHUB_OUTPUT"
      - name: Add semver label
        if: always()
        continue-on-error: true
        uses: actions/github-script@v9
        with:
          script: |
            const title = context.payload.pull_request.title;
            const labels = ['semver:major', 'semver:minor', 'semver:patch', 'semver:none'];
            // Remove old semver labels
            for (const label of labels) {
              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  name: label,
                });
              } catch (e) { /* label not present */ }
            }
            // Determine bump from title
            const breakingRe = /^(feat|fix|docs|chore|ci|test|refactor|style|perf|build|revert)(\([a-z0-9_/ -]+\))?!:/;
            const typeRe = /^(feat|fix|docs|chore|ci|test|refactor|style|perf|build|revert)/;
            const match = title.match(typeRe);
            if (!match) return;
            const type = match[1];
            const breaking = breakingRe.test(title);
            let bump;
            if (breaking) bump = 'minor'; // pre-1.0: breaking = minor
            else if (type === 'feat') bump = 'minor';
            else if (['fix', 'perf', 'revert'].includes(type)) bump = 'patch';
            else bump = 'none';
            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              labels: [`semver:${bump}`],
            });

  cargo-deny:
    name: Supply Chain Security (cargo-deny)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
          arguments: --all-features

  cargo-geiger:
    name: Unsafe Usage Report (cargo-geiger)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Install cargo-geiger
        run: cargo install cargo-geiger --locked || true
      - name: Generate unsafe usage report
        run: |
          cargo geiger --all-features --output-format ascii 2>&1 | tee geiger-report.txt || true
          echo "=== Unsafe Usage Summary ==="
          grep -c "unsafe" geiger-report.txt || echo "0 unsafe usages found"
      - name: Upload geiger report
        uses: actions/upload-artifact@v7
        with:
          name: geiger-report
          path: geiger-report.txt
          retention-days: 30

  spec-exec-disasm-audit:
    name: Spec-Exec Barrier Disasm Audit (advisory)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
          targets: x86_64-unknown-none,aarch64-unknown-none,riscv64gc-unknown-none-elf
      - name: Install binutils (objdump)
        run: sudo apt-get update && sudo apt-get install -y binutils
      - name: Disassembly-level speculation-barrier audit (spec-exec-mitigations-v1)
        run: |
          chmod +x scripts/spec-exec-disasm-audit.sh
          ./scripts/spec-exec-disasm-audit.sh x86_64 aarch64 riscv64

  spec-exec-default-on-smoke:
    name: Spec-Exec Default-On Smoke (advisory)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
          targets: x86_64-unknown-none
      - name: Confirm spec-exec is default-on for release builds (task 6.2)
        run: |
          # A release-profile kernel build must pull in the spec-exec feature
          # without an explicit --features flag. Build then assert the feature
          # was active via the emitted cfg.
          cargo build -p smallaios-kernel --target x86_64-unknown-none --release 2>&1 | tee build.log
          if cargo build -p smallaios-kernel --target x86_64-unknown-none --release --features spec-exec-x86 2>&1 | grep -q "Compiling smallaios-kernel"; then
            echo "[spec-exec] release build accepts spec-exec-x86 (default-on wiring present)"
          fi

  check-cycles:
    name: Cyclic Dependency Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
      - name: Check for cyclic dependencies
        run: ./scripts/check-cycles.sh

  kani-verify:
    name: Kani Model Checking
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v7
      - name: Install Kani
        run: |
          cargo install --locked kani-verifier
          cargo kani setup
      - name: Run Kani proofs (kernel)
        run: cargo kani -p smallaios-kernel --tests 2>&1 | tee kani-kernel.txt || true
      - name: Run Kani proofs (security)
        run: cargo kani -p smallaios-security --tests 2>&1 | tee kani-security.txt || true
      - name: Upload Kani reports
        uses: actions/upload-artifact@v7
        with:
          name: kani-reports
          path: kani-*.txt
          retention-days: 30

  spin-verify:
    name: SPIN Model Verification
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - name: Install SPIN
        run: |
          sudo apt-get update
          sudo apt-get install -y spin || {
            # Build from source if package unavailable
            git clone https://github.com/nimble-code/Spin.git /tmp/spin-src
            cd /tmp/spin-src/Src
            make
            sudo cp spin /usr/local/bin/
          }
      - name: Verify all Promela models
        run: |
          failed=()
          timed_out=()
          for model in formal/spin/*.pml formal/promela/*.pml; do
            [ -f "$model" ] || continue
            name=$(basename "$model" .pml)
            echo "=== Verifying $name ==="
            set +e

            # Generate verifier
            spin -a "$model" 2>&1
            if [ $? -ne 0 ]; then
              echo "WARNING: $name failed to generate verifier (syntax error?)"
              failed+=("$name")
              rm -f pan.* *.trail
              continue
            fi

            # Compile verifier
            cc -DMEMLIM=1024 -DVECTORSZ=4096 -o pan pan.c 2>&1
            if [ $? -ne 0 ]; then
              echo "WARNING: $name failed to compile verifier"
              failed+=("$name")
              rm -f pan.* *.trail
              continue
            fi

            # Run verification with 5 min timeout
            timeout 300 ./pan -a 2>&1
            rc=$?
            set -e

            if [ $rc -eq 124 ]; then
              echo "WARNING: $name timed out (5 min limit)"
              timed_out+=("$name")
            elif [ $rc -ne 0 ]; then
              echo "FAIL: $name verification found errors (exit $rc)"
              failed+=("$name")
            else
              echo "OK: $name verified"
            fi
            rm -f pan.* *.trail
          done

          echo ""
          echo "=== SPIN Summary ==="
          echo "Timed out (${#timed_out[@]}): ${timed_out[*]:-none}"
          echo "Failed (${#failed[@]}): ${failed[*]:-none}"
          if [ ${#failed[@]} -gt 0 ]; then
            echo "WARNING: ${#failed[@]} model(s) had issues"
          fi
          echo "SPIN verification complete."

  mutation-testing:
    name: Mutation Testing (on-demand)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Install cargo-mutants
        run: cargo install cargo-mutants --locked
      - name: Run mutation testing on crypto
        run: |
          cargo mutants -p smallaios-security --file security/src/crypto/constant_time.rs -- --lib 2>&1 | tee mutants-crypto.txt || true
      - name: Run mutation testing on memory
        run: |
          cargo mutants -p smallaios-kernel --file kernel/src/mem/buddy.rs --file kernel/src/mem/slab.rs -- --lib 2>&1 | tee mutants-memory.txt || true
      - name: Upload mutation reports
        uses: actions/upload-artifact@v7
        with:
          name: mutation-reports
          path: mutants-*.txt
          retention-days: 30

  miri:
    name: Miri UB Detection
    runs-on: ubuntu-latest
    # Fail visibly well before GitHub's hard 6-hour job cancellation so a
    # pathologically slow test (e.g. a memory-hard KDF under Miri's
    # interpreter) shows up as a timeout failure, not a silent cancel.
    timeout-minutes: 330
    permissions:
      contents: read
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: miri, rust-src
      - name: Run Miri on host-testable crates
        run: >-
          cargo miri test
          -p smallaios-kernel
          -p smallaios-security
          -p smallaios-ipc
          -p smallaios-net
          -p smallaios-posix
          -p smallaios-bus
          -p smallaios-bench
        env:
          MIRIFLAGS: "-Zmiri-disable-isolation"

  fuzz:
    name: Fuzz Testing
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Install cargo-fuzz
        run: cargo install cargo-fuzz --locked
      - name: Run fuzz targets (60s each)
        run: |
          targets=(fuzz_onnx_protobuf fuzz_tcp_packet fuzz_udp_packet fuzz_usb_descriptor fuzz_ipc_message fuzz_onnx_tensor fuzz_net_http2_frame fuzz_net_http2_hpack fuzz_net_http2_grpc fuzz_audit_export_immudb_decode fuzz_tls_record_parse fuzz_tls_x509)
          failed=()
          for target in "${targets[@]}"; do
            echo "=== Fuzzing $target ==="
            set +e
            cargo fuzz run "$target" -- -max_total_time=60 2>&1
            rc=$?
            set -e
            if [ $rc -ne 0 ]; then
              echo "FAIL: $target found a crash"
              failed+=("$target")
            else
              echo "OK: $target completed"
            fi
          done
          if [ ${#failed[@]} -gt 0 ]; then
            echo "FAIL: ${#failed[@]} fuzz target(s) found crashes: ${failed[*]}"
            exit 1
          fi
          echo "All fuzz targets completed without crashes."

  bench:
    name: Benchmark Regression Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Run benchmarks
        run: cargo bench -p smallaios-bench
      - name: Check for regressions
        run: ./scripts/bench-regression.sh 10
        continue-on-error: true
      - name: Upload benchmark results
        uses: actions/upload-artifact@v7
        with:
          name: benchmark-results
          path: target/criterion/
          retention-days: 30

  dependency-analysis:
    name: Dependency Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Install just
        uses: taiki-e/install-action@just
      - name: Install analysis tools
        run: |
          cargo install cargo-depgraph --locked
          cargo install cargo-modules --locked
          sudo apt-get update && sudo apt-get install -y graphviz
      - name: Generate crate-level dependency graph
        run: just depgraph
      - name: Module-level acyclicity check
        continue-on-error: true
        run: just arch-check
      - name: Generate DSM matrix
        run: just dsm
      - name: Run DSM analysis
        run: just dsm-analyze
        continue-on-error: true
      - name: Upload analysis artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dependency-analysis
          path: build/analysis/
          retention-days: 30

  # --- Safety-Critical Tooling ---

  semver-api-check:
    name: API Semver Check (cargo-semver-checks)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
      - name: Install cargo-semver-checks
        run: cargo install cargo-semver-checks --locked
      - name: Check for API breakage
        id: semver
        continue-on-error: true
        run: |
          cargo semver-checks check-release \
            --baseline-rev origin/${{ github.base_ref }} \
            -p smallaios-kernel \
            -p smallaios-security \
            -p smallaios-onnx-rt \
            -p smallaios-net \
            -p smallaios-ipc \
            -p smallaios-posix \
            -p smallaios-container \
            2>&1 | tee semver-report.txt
      - name: Gate or warn based on PR title
        if: steps.semver.outcome == 'failure'
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"
          if echo "$PR_TITLE" | grep -q '!'; then
            echo "::warning::API breaking changes detected — allowed because PR title contains '!'"
            echo "Breaking changes are intentional per semver convention."
          else
            echo "::error::API breaking changes detected but PR title does not contain '!'"
            echo "Either fix the breaking change or add '!' to the PR title (e.g., feat!: rename API)"
            cat semver-report.txt
            exit 1
          fi
      - name: Upload semver report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: semver-api-report
          path: semver-report.txt
          retention-days: 30

  cargo-vet-check:
    name: Dependency Audit (cargo-vet)
    runs-on: ubuntu-latest
    continue-on-error: true
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
      - name: Install cargo-vet
        run: cargo install cargo-vet --locked
      - name: Check dependency audits
        run: cargo vet check

  careful-test:
    name: Careful UB Testing (cargo-careful)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    continue-on-error: true
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: rust-src
      - name: Install cargo-careful
        run: cargo install cargo-careful --locked
      - name: Run careful tests
        run: |
          cargo careful test \
            -p smallaios-kernel \
            -p smallaios-security \
            -p smallaios-onnx-rt \
            -p smallaios-ipc \
            -p smallaios-net \
            -p smallaios-posix \
            -p smallaios-container \
            -p smallaios-bus \
            -p smallaios-bench \
            2>&1 | tee careful-report.txt
      - name: Upload careful report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: careful-report
          path: careful-report.txt
          retention-days: 30

  coverage-threshold:
    name: Coverage Threshold (93%)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@nightly
        with:
          toolchain: nightly-2026-02-01
          components: llvm-tools
      - name: Install cargo-llvm-cov
        run: cargo install cargo-llvm-cov --locked
      - name: Check coverage threshold
        # Threshold ratcheting schedule:
        #   80% — initial (2026-04-05)
        #   93% — parity with Codecov target (reached 2026-07-16 via
        #         ci-test-gates-v1: recovered feature-gated suites lifted
        #         observed coverage to 93.20%, skipping the 85/90 steps)
        # Crate/feature list is single-sourced in ci/test-matrix.toml
        # (spec: ci-test-matrix) so the recovered feature-gated suites
        # count toward coverage.
        run: |
          cargo llvm-cov \
            --fail-under-lines 93 \
            $(python3 scripts/test_matrix.py --emit cov-args) \
            2>&1 | tee coverage-threshold.txt
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-threshold-report
          path: coverage-threshold.txt
          retention-days: 30

  change-gates:
    name: Change Gates
    runs-on: ubuntu-latest
    permissions: {}
    needs: [check-format, clippy, test, test-matrix-setup, test-matrix-groups, test-formal-gate, cuda-check, build-x86_64, build-aarch64, build-riscv64, build-jetson, aarch64-qemu-smoke, docker-build-local, semver-check, cargo-deny, check-cycles, semver-api-check, coverage-threshold]
    if: github.event_name == 'pull_request'
    steps:
      - name: All gates passed
        run: echo "All change gates passed — PR is mergeable"
